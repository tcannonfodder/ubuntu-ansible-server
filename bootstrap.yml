---
- hosts: all
  vars_files:
    - vars.yml
  roles:
  - { role: common }

- hosts: webserver
  roles:
  - { role: nginx }

- hosts: staticsite
  roles:
  - { role: staticsite-base }
  - { role: staticsite-nginx }
  vars_files:
    - staticsite_vars.yml